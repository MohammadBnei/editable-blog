apiVersion: v1
kind: Service
metadata:
  name: editable-blog-service
spec:
  selector:
    app: editable-blog # Match labels from the deployments
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: blog-certificate
spec:
  commonName: blog.bnei.dev
  secretName: blog-certificate-secret
  dnsNames:
    - 'blog.bnei.dev'
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod

---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: editable-blog
spec:
  entryPoints:
    - websecure
    - web
  routes:
    - kind: Rule
      match: Host(`blog.bnei.dev`)
      middlewares:
        - name: compress
      services:
        - kind: Service
          name: editable-blog-service
          namespace: editable-blog
          port: 80
  tls:
    secretName: blog-certificate-secret

---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: compress
spec:
  compress: {}
