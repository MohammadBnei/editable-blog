apiVersion: apps/v1
kind: Deployment
metadata:
  name: editable-blog
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: editable-blog
  template:
    metadata:
      labels:
        app: editable-blog
    spec:
      containers:
        - image: mohammaddocker/editable-blog
          name: editable-blog
          ports:
            - containerPort: 3000
          env:  
            - name: PORT
              value: "3000"
          volumeMounts:
            - name: data
              mountPath: /app/data/
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: sqlite3